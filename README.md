# frozen_elixir

Freeze your Phoenix web application into a static site, similar to [Frozen-Flask](https://github.com/Frozen-Flask/Frozen-Flask). Currently developing as a custom mix task inside the `frozen` application.
